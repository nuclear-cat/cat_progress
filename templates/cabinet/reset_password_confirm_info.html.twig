{% extends 'cabinet/base.html.twig' %}

{% block body %}
    <div>
        <div class="container">
            <div class="row justify-content-center align-items-center">
                <div class="col-4 mt-5 card p-4">
                    <div class="mb-3">Check your email for a link to reset your password. If it doesn’t appear within a few minutes, check your spam folder.</div>
                    <a class="btn btn-primary" href="{{ path('cabinet.login') }}">Return to log in</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}