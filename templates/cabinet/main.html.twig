{% extends 'cabinet/base.html.twig' %}

{% block title %}Tasks{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('cabinet.home') }}">
                <img width="30" alt="Logo" src="{{ asset('/icon-white.svg') }}"/>
            </a>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('cabinet.habit.calendar') }}">Calendar</a>
                </li>
            </ul>

            <div class="d-flex">
                <ul class="main-navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            {{ app.user.name }}
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <li><a class="dropdown-item" href="{{ path('cabinet.category.list') }}">Categories</a></li>
                            <li><a class="dropdown-item" href="{{ path('cabinet.habit.list') }}">Habits</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    {% block content %}
    {% endblock %}

{% endblock %}
